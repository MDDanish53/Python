'''
if we want to update value of a existing key then follow:

dict_name[key_name(access the existing key)] = new_value

'''

my_dict = {
    "language": "Python",
    "experience": "Begineer"
}

print(my_dict)

my_dict["experience"] = "Intermediate"

print(my_dict)